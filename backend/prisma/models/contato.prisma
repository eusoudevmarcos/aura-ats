model Contato {
    id       String  @id @default(uuid(7))
    telefone String?
    whatsapp String?
    email    String?

    candidatoId String?
    candidato   Candidato? @relation(fields: [candidatoId], references: [id])

    @@index([candidatoId])
    @@schema("Contato")
}
